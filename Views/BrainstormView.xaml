<UserControl x:Class="MarkFlow.Views.BrainstormView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                <Button Content="노드 생성" Click="CreateNode_Click"
                    Padding="12,6" Margin="0,0,8,0"
                    Background="#171717" Foreground="White" BorderThickness="0"/>
                <Button Content="저장" Click="Save_Click"
                    Padding="12,6" Margin="0,0,8,0"
                    Background="Transparent" BorderBrush="#E0E0E0" BorderThickness="1"/>
                <Button Content="연결 초기화" Click="ClearConnections_Click"
                    Padding="12,6"
                    Background="Transparent" BorderBrush="#E0E0E0" BorderThickness="1"/>
                <TextBlock Text="* 노드 클릭 → 다른 노드 클릭으로 연결"
                    FontSize="12" Foreground="#888" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1"
            HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
            PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <Canvas x:Name="MainCanvas" Width="3000" Height="2000" Background="#F8F8F8"/>
        </ScrollViewer>
    </Grid>
</UserControl>